import React from 'react';
import { registerRootComponent } from 'expo';
import StackNavigator from '../StackNavigator'; // Modifiez le chemin d'importation

registerRootComponent(StackNavigator);
